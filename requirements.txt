# Core (required to run training, API and app)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
xgboost>=2.0
shap>=0.44
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
jinja2>=3.1                    # server-rendered web pages (auth site)
python-multipart>=0.0.9       # form + file upload parsing for the web app
streamlit>=1.30
plotly>=5.18
joblib>=1.3
matplotlib>=3.7
openpyxl>=3.1                  # read .xlsx uploads in the auto-ingest pipeline

# Optional: enable LLM credit memos (otherwise a template memo is used)
# openai>=1.30
# anthropic>=0.25

# Dev/test
pytest>=7.4
